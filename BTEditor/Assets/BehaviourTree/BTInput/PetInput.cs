using UnityEngine;
using System.Collections;
//using battle;

namespace GameAI
{
    public class PetInput : BTreeInput
    {
        //public void Init(Character unit, Character parent, float minSpeed, float maxSpeed)
        //{
        //    //_unit = unit;
        //    //_parent = parent;
        //    //_direction = new Vector3(-1, 0, 0);
        //    //_tree = BLRoleAILib.InstantiateTree(BLRoleAILib.TreeType.RunSummon, this);
        //    //_screenRect = new Rect(0, 0, Screen.width * 1.2f, int.MaxValue);
        //    //_speed = BattleUtil.Random(minSpeed, maxSpeed);

        //    //_runCommand = new SummonRunCommand(_unit, _direction, _speed);
        //    SetCharacter(unit);
        //    SetParent(parent);
        //    SetRunMaxSpeed(maxSpeed);
        //    SetRunMinSpeed(minSpeed);
        //}
        
    }

}
